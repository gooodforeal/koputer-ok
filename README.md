# OAuth2 Google Authentication с FastAPI и React

Полноценная система авторизации с использованием Google OAuth2, FastAPI для бэкенда и React для фронтенда.

## 🚀 Возможности

- ✅ Авторизация через Google OAuth2
- ✅ JWT токены для аутентификации
- ✅ Защищенные маршруты
- ✅ Современный React интерфейс
- ✅ CORS настроен для разработки
- ✅ PostgreSQL база данных с **asyncpg** для максимальной производительности
- ✅ Полная асинхронная работа с базой данных
- ✅ Repository паттерн для работы с БД
- ✅ Полное покрытие тестами (Unit, Integration, Performance, Security)
- ✅ Простая архитектура без лишних зависимостей
- ✅ Автоматизированное тестирование в CI/CD

## 📋 Требования

- Python 3.8+
- Node.js 16+
- PostgreSQL

## 🛠 Установка и настройка

### 1. Настройка Google OAuth2

1. Перейдите в [Google Cloud Console](https://console.cloud.google.com/)
2. Создайте новый проект или выберите существующий
3. Включите Google+ API
4. Создайте OAuth 2.0 credentials:
   - Application type: Web application
   - Authorized redirect URIs: `http://localhost:8000/auth/google/callback`
5. Скопируйте Client ID и Client Secret

### 2. Настройка базы данных

```bash
# Создайте базу данных PostgreSQL
createdb oauth_db

# Или используйте Docker
docker run --name postgres-oauth -e POSTGRES_DB=oauth_db -e POSTGRES_USER=user -e POSTGRES_PASSWORD=password -p 5432:5432 -d postgres:13
```

### 3. Настройка бэкенда

```bash
# Клонируйте репозиторий
cd oauth-google

# Создайте виртуальное окружение
python -m venv venv

# Активируйте виртуальное окружение
# Windows:
venv\\Scripts\\activate
# Linux/Mac:
source venv/bin/activate

# Установите зависимости
pip install -r requirements.txt

# Создайте файл .env на основе env.example
cp env.example .env

# Отредактируйте .env файл с вашими настройками
```

### 4. Настройка фронтенда

```bash
# Перейдите в папку фронтенда
cd frontend

# Установите зависимости
npm install

# Создайте файл .env на основе env.example
cp env.example .env
```

## 🔧 Конфигурация

### Файл .env (бэкенд)

```env
# Database
DATABASE_URL=postgresql://user:password@localhost/oauth_db


# Google OAuth2
GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_SECRET=your_google_client_secret
GOOGLE_REDIRECT_URI=http://localhost:8000/auth/google/callback

# JWT
SECRET_KEY=your-secret-key-here
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Frontend
FRONTEND_URL=http://localhost:3000

# Environment
ENVIRONMENT=development
```

### Файл .env (фронтенд)

```env
REACT_APP_API_URL=http://localhost:8000
```

## 🚀 Запуск приложения

### Запуск бэкенда

```bash
# Активируйте виртуальное окружение
venv\\Scripts\\activate  # Windows
# source venv/bin/activate  # Linux/Mac

# Запустите сервер
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

### Запуск фронтенда

```bash
# В новом терминале
cd frontend
npm start
```

Приложение будет доступно по адресам:
- Фронтенд: http://localhost:3000
- Бэкенд API: http://localhost:8000
- API документация: http://localhost:8000/docs

## 📚 API Endpoints

### Аутентификация

- `GET /auth/google` - Получить URL для авторизации через Google
- `GET /auth/google/callback` - Callback для Google OAuth2
- `GET /auth/me` - Получить информацию о текущем пользователе
- `POST /auth/logout` - Выход из системы

### Пользователи

- `GET /users/profile` - Профиль пользователя
- `GET /users/protected` - Пример защищенного маршрута

## 🏗 Архитектура

### Бэкенд (FastAPI)

```
app/
├── __init__.py
├── main.py              # Основное приложение
├── config.py            # Конфигурация
├── database.py          # Настройка БД
├── auth.py              # JWT аутентификация
├── oauth.py             # Google OAuth2
├── dependencies.py      # FastAPI зависимости
├── models/              # SQLAlchemy модели
│   ├── __init__.py
│   ├── base.py          # Базовая модель
│   └── user.py          # Модель пользователя
├── schemas/             # Pydantic схемы
│   ├── __init__.py
│   ├── user.py          # Схемы пользователя
│   ├── auth.py          # Схемы аутентификации
│   └── common.py        # Общие схемы
├── repositories/        # Repository паттерн
│   ├── __init__.py
│   ├── base.py          # Базовый репозиторий
│   └── user_repository.py # Репозиторий пользователей
└── routers/
    ├── __init__.py
    ├── auth.py          # Маршруты аутентификации
    └── users.py         # Маршруты пользователей
```

### Фронтенд (React)

```
frontend/src/
├── index.tsx            # Точка входа
├── App.tsx              # Основной компонент
├── index.css            # Глобальные стили
├── types/
│   └── auth.ts          # TypeScript типы
├── contexts/
│   └── AuthContext.tsx  # Контекст аутентификации
├── services/
│   └── api.ts           # API клиент
└── components/
    ├── Login.tsx        # Компонент входа
    ├── Profile.tsx      # Профиль пользователя
    ├── Dashboard.tsx    # Панель управления
    ├── ProtectedRoute.tsx # Защищенные маршруты
    └── AuthCallback.tsx # Обработка callback
```

## 🏛 Архитектурные паттерны

### Repository Pattern
Проект использует **Repository Pattern** для работы с базой данных:

**Преимущества:**
- **Инкапсуляция** - вся логика работы с БД в одном месте
- **Тестируемость** - легко создавать моки для тестов
- **Переиспользование** - один репозиторий для разных роутеров
- **Типизация** - полная поддержка Python типов

### Организация кода
**Модели** (`app/models/`):
- `user.py` - модель пользователя
- `base.py` - базовая модель с общими полями

**Схемы** (`app/schemas/`):
- `user.py` - схемы для пользователей (UserCreate, UserResponse, UserUpdate, UserStats)
- `auth.py` - схемы для аутентификации (Token, GoogleUserInfo, LoginResponse)
- `common.py` - общие схемы (MessageResponse, ErrorResponse, PaginationParams)

**Репозитории** (`app/repositories/`):
- `user_repository.py` - репозиторий для работы с пользователями
- `base.py` - базовый репозиторий с общими методами

### Пример использования:
```python
# В роутере
@router.get("/users/", response_model=List[UserResponse])
async def get_users(user_repo: UserRepository = Depends(get_user_repository)):
    return user_repo.get_all(skip=0, limit=100)

# В тестах
def test_create_user(user_repository):
    user_data = UserCreate(email="test@example.com", ...)
    user = user_repository.create(user_data)
    assert user.email == "test@example.com"
```

## 🔒 Безопасность

- JWT токены с истечением срока действия
- CORS настроен для разработки
- Валидация данных с Pydantic
- Защищенные маршруты с middleware
- Безопасное хранение токенов в localStorage

## 🧪 Тестирование

```bash
# Запуск тестов бэкенда
pytest

# Запуск тестов с подробным выводом
pytest -v

# Запуск конкретного теста
pytest tests/test_user_repository.py::TestUserRepository::test_create_user

# Запуск тестов фронтенда
cd frontend
npm test
```

## 📦 Развертывание

### Docker (Рекомендуемый способ)

Полная настройка Docker для запуска всего приложения одной командой:

```bash
# 1. Создайте .env файл с вашими настройками
cp env.example.txt .env
# Отредактируйте .env с вашими Google OAuth credentials

# 2. Запустите все сервисы одной командой
docker-compose up --build

# Или используйте готовые скрипты:
# Windows:
start-docker.bat
# Linux/Mac:
./start-docker.sh
```

Приложение будет доступно:
- Frontend: http://localhost:3000
- Backend API: http://localhost:8000
- API Docs: http://localhost:8000/docs

**Полезные команды:**
```bash
# Запуск в фоне
docker-compose up -d --build

# Просмотр логов
docker-compose logs -f

# Остановка
docker-compose down

# Остановка с удалением данных
docker-compose down -v
```

Подробнее см. [DOCKER.md](DOCKER.md)

## 🧪 Тестирование

### Быстрый старт
```bash
# Установка тестовых зависимостей
python install_test_deps.py

# Запуск всех тестов
make test

# Только синхронные тесты (рекомендуется для начала)
make test-sync
```

### Типы тестов
- **Unit тесты** - Отдельные компоненты API
- **Интеграционные тесты** - Взаимодействие компонентов
- **Тесты производительности** - Время ответа, память
- **Тесты безопасности** - Защита от атак
- **Асинхронные тесты** - Конкурентные операции

### Покрытие кода
```bash
# Запуск с покрытием кода
make test-coverage

# HTML отчет
make test-html
```

### Документация по тестированию
- [QUICK_START_TESTS.md](QUICK_START_TESTS.md) - Быстрый старт
- [TESTING.md](TESTING.md) - Подробная документация
- [TROUBLESHOOTING.md](TROUBLESHOOTING.md) - Устранение неполадок

### Heroku

```bash
# Установите Heroku CLI
# Создайте приложение
heroku create your-app-name

# Настройте переменные окружения
heroku config:set GOOGLE_CLIENT_ID=your_client_id
heroku config:set GOOGLE_CLIENT_SECRET=your_client_secret
# ... другие переменные

# Разверните
git push heroku main
```

## 🤝 Вклад в проект

1. Fork репозиторий
2. Создайте feature branch
3. Commit изменения
4. Push в branch
5. Создайте Pull Request

## 📄 Лицензия

MIT License

## 🆘 Поддержка

Если у вас возникли проблемы:

1. Проверьте логи бэкенда и фронтенда
2. Убедитесь, что все переменные окружения настроены
3. Проверьте, что Google OAuth2 credentials корректны
4. Убедитесь, что база данных доступна

## 🔄 Обновления

Для обновления зависимостей:

```bash
# Бэкенд
pip install -r requirements.txt --upgrade

# Фронтенд
cd frontend
npm update
```

